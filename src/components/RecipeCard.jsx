import React, { useState } from "react";
import { Card, CardActions, CardContent, Tyopgraphy } from "@mui/material";

const RecipeCard = () => {
  return (
    <Card>
      <CardContent>
        <Typography></Typography>
      </CardContent>
    </Card>
  );
};

export default RecipeCard;